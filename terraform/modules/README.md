# Terraform Modules

Reusable, composable Terraform/OpenTofu modules.

Each module should be self-contained and versionable.

## Available Modules

- `vm/`: Provider-agnostic baseline VM contract.
- `windows-vm/`: Provider-agnostic Windows VM contract with first-boot WinRM and administrator bootstrap inputs.
