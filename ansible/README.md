# Ansible

Configuration management and post-provisioning automation.

- `roles/` for reusable role logic.
- `inventories/` for target definitions per environment.
- `playbooks/` for executable orchestration.
